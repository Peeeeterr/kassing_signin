# 项目发行版与文档规范 (Release & Documentation Guidelines)

本规范用于指导本项目（kassing-signin）后续所有发行版构建、说明文档编写及用户交互设计。

## 1. 文档编写核心逻辑

1. **面向普通非技术用户**：
   - 避免展示底层复杂指令与代码细节（如底层 crontab 语法、环境变量名等）；
   - 状态提示一律采用生活化通俗表达（例如：“工作日 (周一至周五) 07:32 自动打卡”、“放假/调休模式”）；
   - 全文严禁使用 Emoji 表情符号，保持专业、整洁。

2. **自愈环境免手动安装**：
   - 不在文档中罗列 `pip install -r requirements.txt` 等手动依赖安装步骤；
   - 首次启动统一由 `run.sh` / `run.bat` 自动检测并自愈安装；
   - 核心步骤简洁归纳为两步：
     - 步骤一：一键运行与初始化向导（初次运行由向导生成 `.env`）；
     - 步骤二：日常运行与控制台管理（启动统一控制台菜单）。

3. **跨平台对齐与参数简洁化**：
   - 双端并重：Linux/macOS 推荐 `./run.sh`，Windows 推荐双击 `run.bat`；
   - 进阶命令行参数章节：不绑定单一操作系统前缀，仅列出通用的 `-` 参数及其简明用途，不加冗余括号解释；
   - 彻底剔除面向开发调试的参数（如 `--dry-run`）。

## 2. 发行版构建与分发规范

1. **零冗余与无敏感信息**：
   - 绝不打包包含个人凭据的真实 `.env`，仅保留 `.env.example`；
   - 排除内部调试脚本（如 `scripts/` 目录）与开发虚拟环境（`.venv/`）；
   - 排除 Python 编译缓存文件（`*.pyc`、`__pycache__/`）。

2. **保留完整运行骨架**：
   - 必须保留运行依赖的基础目录：`PhotoStorage/`（含新手提示文本文档）、`Archives/`、`logs/`；
   - 必须确保 `run.sh` 与 `run_cron.sh` 具备可执行权限（`chmod +x`）。
