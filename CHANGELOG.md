# 更新日志 (CHANGELOG)

本项目遵循语义化版本规范。

## [v1.2.1] - 2026-09-11

Windows 批处理原生解析与闪退根治、控制台代码页自愈与执行日志编码统一治理、交互向导文案精简与平滑过渡体验重构。

### 修复 (Fixed)
- **Windows 批处理解析致命错误与闪退彻底根治 (`run.bat`)**：
  - 彻底修复因输出文案包含 `(requirements.txt)...` 等嵌套圆括号，导致 Windows `cmd.exe` 预编译器将右括号误识别为 `if (...)` 代码块提前闭合，进而抛出 `... was unexpected at this time.`（中文系统对应 `此时不应有 ...。`）并瞬时强制闪退的深层顽疾；
  - 彻底移除复合嵌套括号代码块，改用高稳健性的原生扁平化跳转逻辑 (`goto`)；
  - 移除了有歧义的 `if errorlevel 0 if not errorlevel 1` 链式语法，改用原生平铺的 Python 探测链；
  - 全面规范 Python 解释器调用，所有 `%PYTHON_EXE%` 变量调用均严格使用双引号包裹，彻底解决因 Windows 用户名或安装目录包含空格（如 `C:\Users\John Doe\`）导致的命令截断执行失败；
  - 移除了菜单栏所有可能被 CMD 误识别为进程管道的特殊符号（如 `|`），彻底杜绝 `'可输入' is not recognized` 等语法解析错误；
  - 增加回车空输入容错保护（直接平滑刷新主界面）与操作完成任意键暂停 (`pause >nul`)，杜绝结果信息一闪而过。
- **控制台代码页污染与主菜单乱码自愈治理**：
  - 修复运行定时器健康体检 (`scripts/test_timer.py` / 菜单选项 [12]) 仿真调用 `run_windows.bat` 后，因修改代码页导致返回主菜单显示火星文乱码（如 `ѧ (kassing-signin) ̨`）的问题；
  - 在 `run.bat` 启动与主菜单循环入口增加 `chcp 936` 强制自愈机制，无论外部环境或子进程如何切换，返回主菜单一律自动锁定回 Windows 原生中文代码页 (GBK 936)；
  - `run_windows.bat` 彻底剔除全局代码页修改 (`chcp 65001`)，首尾包裹提示统一采用纯 ASCII 英文，避免对外部终端环境产生侵入式副作用。
- **日志混合编码导致记事本乱码问题治理 (`logs/cron.log`)**：
  - 修复 `run_windows.bat` 头部输出 GBK 与 Python 主体输出 UTF-8 混合写入日志，导致 Windows 记事本自动将整份文件误判为 ANSI/GBK 并将中间打卡信息显示为乱码（如 `瀛︽惌瀛?`、`姝ｅ湪杩炴帴`）的现象；
  - 规范后台批处理日志包裹层采用纯 ASCII 英文，与 Python 输出的 UTF-8 编码实现 100% 格式无缝兼容；
  - `main.py` 内部查看日志功能升级为 UTF-8 与 GBK 自适应双解码兼容引擎。

### 变更与优化 (Changed & Improved)
- **初始化与定时配置向导体验重构 (`main.py`)**：
  - 全面精炼初始化向导 (`-init`) 与交互式定时打卡配置 (`-setup-cron`) 的文案，去芜存菁，字数削减约 60%，消除冗长文字带来的认知负担；
  - 新增步骤间平滑过渡与视觉清屏机制：用户每次输入完成敲击回车后，系统停留 0.5 秒平滑清屏，并以醒目步骤栏拉起下一步输入，交互节奏清晰明朗。
- **新增 Windows 运行兜底保护脚本 (`windows.bat`)**：
  - 新增 `windows.bat` 包装入口，在调用 `run.bat` 的基础上捕获任何非零退出码并强制 `pause` 暂停，方便用户排查罕见的系统环境故障，彻底告别无提示闪退。
- **版本号同步升级**：
  - `main.py`、`README.md` 与项目元数据版本号统一对齐至 `v1.2.1`。

## [v1.2.0] - 2026-09-11

智能定时跳过打卡机制、定时器全链路健康体检诊断工具、控制台菜单序号严格自增重构、交互文案精炼去括号及用户体验全面升级。

### 新增 (Added)
- **单次/计数跳过打卡功能 (`-skip` / `--skip [N]` / 控制台 [9])**：
  - **参数化单次/多次跳过**：以传入参数 `count: int = 1` 为底层核心实现，默认跳过单次打卡，同时天然支持传入任意正整数以指定跳过次数（如 `--skip 3`）；
  - **自动递减与自愈恢复**：打卡触发时若检测到跳过设置，自动安全退出并递减剩余次数，配额用尽后无缝自愈恢复正常打卡，彻底解决传统“长假暂停后忘记恢复”的痛点；
  - **多端状态联动与保护**：控制台面板顶栏动态提示跳过生效状态与剩余次数，定时器诊断脚本 (`test_timer.py`) 纳入跳过状态检测，演练模式 (`--dry-run`) 保护不消耗跳过配额，`--force` 强制打卡可快速绕过，`-resume` / 控制台 [5] 一键清除暂停与跳过；
  - **交互式向导配置**：新增 `main.py --skip-interactive` 命令并在控制台设置入口，支持直接回车默认跳过 1 次（单次）或输入自定义正整数次数。
- **系统定时器全套健康诊断与体检 (`scripts/test_timer.py` / `main.py -test-cron` / 控制台 [13])**：
  - 针对定时器未能按预期工作的各类高频根因提供 6 大项全自动体检：
    1. 守护进程状态：检测 Linux cron/crond 与 Windows Task Scheduler 服务是否在后台运行，并提供自愈指引；
    2. 时区时钟校验：检测本地系统时钟与东八区北京时间 (UTC+8) 的偏移，预警因缺少 CRON_TZ 导致的时区偏差；
    3. 定时规则注册：检测是否在系统中实际写入定时任务；
    4. 脚本执行权限与 Python 环境：检测 `run_cron.sh` 可执行权限 (+x) 及 Python 虚拟环境依赖；
    5. 放假/暂停/跳过排查：检测 `.pause` 标识文件与 `.skip` 计数配额，避免用户误判“定时器不工作”；
    6. 全链路仿真触发测试：立即唤起后台调度脚本（演练保护模式），验证 Python 调用、日志写入与执行退出码，并自动提取核心阻断原因。
- **控制台快捷交互选项规范重排 (`run.sh` / `run.bat`)**：
  - **常规签到置顶与安全防护**：隐匿直接打卡的「立即签到」显式菜单（仍保留底层 `-y` 参数与控制台隐藏输入），将带缓冲倒计时与取消保护的「常规签到」置顶为 **[1]**；
  - **菜单编号严格连续自增**：控制台 14 项功能严格按照 1 至 14 连续递增排序，彻底消除跳序与混乱；
  - **业务分类归属合理化**：将「跳过下次打卡」规范纳入 `【自动打卡与假期管理】` 编号为 **[8]**，将「测试系统定时器与调度健康度」编排为 **[12]**；
  - **交互文案纯净干练**：全面移除所有菜单项的多余括号解释语句（如去掉 `(直接提交打卡)`、`(带 10 秒缓冲倒计时)`、`(放假/调休跳过打卡)`、`(修改账号密码等)` 等）。

### 优化 (Optimized)
- **项目专业术语规范化**：全局将旧有「底图」统一规范描述为「图片」，「底图池」统一规范描述为「图库」或「图片池」，消解底层计算机术语包袱，使日常交互更加直观自然。
- **交互文案全局去括号与精炼**：
  - 全局遍历所有交互界面，除非必要彻底移除所有说明性的 `(...)` 解释语句；
  - 自动打卡任务设置输出精炼：移除 `(人类易读语义)`、`(工作日)`、`(防风控随机延时 10~180秒)`，采用紧凑清晰的表达（如：`【早读】 每周一至周五 08:00 | 下次预计: 09月14日 周一 08:00，约 67 小时 54 分钟后`）；
  - 测试控制台与向导输出精简：移除 `(Check Status)`、`(Watermark Test)` 等英文后缀及 `(东八区)`、`(ID: xxx)` 等赘述，改用 `|` 或逗号分隔。
- **默认推荐打卡时间点调整**：将自动配置推荐时间点与兜底时间全面统一为 **`08:00, 18:17, 20:42`**。
- **用户称谓字段统一优化**：将全局提示信息、交互向导、鉴权反馈及文档中的 `(工号/学号)` 等字段统一调整为规范标准的 **`用户名`**。
- **定时规则自然语言语义化与下次触发推算**：
  - 将 Linux Crontab 与 Windows 计划任务转换为通俗易懂的自然语言描述；
  - 智能结合当前东八区北京时间与星期规则，动态推算并展示各时段的下次预计触发时刻与倒计时；移除冗余的“最临近触发”状态行，避免在暂停打卡或放假调休时产生误导。
- **控制台与测试交互全流程自动清屏优化**：在控制台菜单（`run.sh`、`run.bat`、`scripts/menu_console.py`）循环及各项功能执行前后引入自动清屏机制，彻底杜绝历史输出与菜单反复堆叠。

### 修复 (Fixed)
- **初始化向导提示文件过滤修复**：修复初始化配置向导扫描 `PhotoStorage/` 目录时将提示文本（如 `请将打卡照放入此目录.txt`）误判为图片并提示是否清空的缺陷，增加严格图片扩展名白名单校验。
- **Windows 计划任务名称解析修复**：修复 `scheduler.py` 中因任务名切分长度判断条件有误导致 Windows 计划任务无法正确解析出时间的缺陷。
- **恢复与调休管理联动优化**：优化 `resume_cron()` 与控制台 [4] 选项，使其一键恢复时同时清理长期暂停 (`.pause`) 与跳过设置 (`.skip`)。
- **关联选项引用同步修正**：全面更新脚本提示、CLI 参数帮助、错误警告及文档中关于控制台选项编号（如 [8] 和 [12]）的指引说明。

---

## [v1.1.1] - 2026-09-11

紧急修复 Windows 环境下批处理换行符与代码页编码错位导致的闪退问题。

### 修复 (Fixed)
- **批处理换行符 CRLF 规范化**：修复跨平台拉取代码时批处理文件被转为 LF 导致 Windows CMD 执行错位报错的问题；新增 `.gitattributes` 强制声明 Windows 脚本采用 CRLF 换行。
- **UTF-8 代码页提前激活**：在所有中文输出前优先执行 `chcp 65001` 并设置 `PYTHONIOENCODING=utf-8`，解决命令解析偏移与乱码闪退。

---

## [v1.1.0] - 2026-09-10

正式体验升级版本，聚焦于交互极简化、通俗化表达与零门槛开箱即用。

### 新增 (Added)
- **统一控制台交互菜单 (`run.sh` / `run.bat`)**：精炼各项直观日常功能（立即签到、常规签到、查看记录、放假暂停、假期恢复、开启/修改定时、关闭定时、查看状态、重新向导、环境修复），操作清晰明确。
- **Python 依赖自动检测与自愈安装**：启动脚本在启动时自动检测核心依赖库（`requests` 与 `Pillow`），若检测到未安装或首次运行，自动执行 `pip install -r requirements.txt` 进行一键环境自愈。
- **今日打卡记录与时段查询 (`-records` / `--records`)**：结构化查询并展示今日所有打卡时段的开放时间窗口、官方签到状态（已打卡/未开始/已截止/开放中）、签到时间、打卡地点以及本地最新生成的防伪水印照片归档。
- **跨平台定时调度管理引擎 (`kassing_signin/scheduler.py`)**：安全读写 Linux Crontab 与 Windows 任务计划程序，内置标记块隔离机制，杜绝误动系统既有其他定时任务。
- **向导打卡时段联网推荐与一键写入**：在 `-init` 向导末尾自动联网拉取实际打卡时段，提供避开高峰的推荐时间，回车即可一键将打卡计划写入操作系统后台定时器。
- **快捷暂停与恢复定时任务 (`-pause` / `-resume`)**：一键暂停定时自动打卡（生成 `.pause` 标识文件），系统定时器触发时将自动安全跳过，放假/调休无需关闭或修改系统的 crontab / 任务计划程序。
- **开发测试工具与演练向导 (`scripts/`)**：在开发测试版中保留全套测试与演练工具集，支持在向导末尾及控制台直接发起打卡全流程演练测试（`--dry-run` 不落库）、独立水印合成校验与账号图库状态诊断。

### 修复 (Fixed)
- **类型提示导入修复**：修复 `main.py` 内部函数签名使用 `Optional` 时未从 `typing` 模块导入，导致模块加载时报 `NameError: name 'Optional' is not defined` 的问题。

### 优化 (Optimized)
- **全流程交互通俗化重构**：消除所有对普通用户晦涩的底层技术字符串（如 `CRON_TZ=Asia/Shanghai`、`32 07 * * 1-5 ...`），自动解析翻译为生活化表达（如“工作日 (周一至周五) 07:32、18:17、20:42 自动打卡”）。
- **极简化向导完成引导**：配置完成后仅呈现直接的统一控制台启动指引（Linux 运行 `./run.sh`，Windows 双击 `run.bat`），移除堆叠的参数列表，专业参数全数沉淀至 README 进阶章节。
- **两阶段前置自检体系（先本地后联网）**：将主程序前置检查逻辑严格划分为「本地离线检测」与「联网鉴权诊断」两大阶段。启动打卡时，优先在本地离线校验 `.env` 凭据有效性与 `PhotoStorage/` 图库容量门槛；全部通过后再发起网络连接进行登录鉴权、身份信息确认与打卡时段就绪诊断。
- **打卡前置鉴权与身份信息确认机制**：打卡前优先发起服务端登录鉴权，提取并确认展示打卡人员姓名、工号/学号、所属组织及绑定手机号，并提前诊断今日打卡任务状态（已完成/尚未开放/已截止）。当凭据错误、时段未开放或今日任务已全部完成时提前友好告知并安全退出，彻底避免用户盲目等待 10 秒倒计时；同时向下透传已鉴权会话与目标时段。
- **全面统一 .env 配置文件**：彻底移除历史遗留的 `.config` 软链接文件及代码中的回退查找逻辑，全局统一规范使用 `.env` 作为唯一的配置文件。
- **控制台状态三态动态检测**：精准呈现三种运行状态（未配置自动打卡 / 自动打卡已开启且正常调度 / 自动打卡已开启但处于放假暂停），并自动提供对应的一键操作指引。

---

## [v1.0.0] - 2026-09-09

首个正式公开版本发布。

### 核心功能
- **全流程自动化**：账号登录鉴权、开放时段智能匹配、图片调度、防伪水印合成、图片上传与打卡闭环。
- **环境初始化向导 (`-init`)**：一键引导用户配置 `.env` 凭据，自动探测当前 Python 环境路径，自动重置并清理历史痕迹。
- **安全极坐标离散定位**：基于允许打卡半径生成均匀分布经纬度并附带精度抖动，避免固定坐标与中心聚簇。
- **动态照片冷却机制**：滑动窗口冷却池（默认最低 9 次，满足 3 天轮转），强制图库图片数量达到「冷却池 + 8 张」，规避图像重复审查。
- **自适应防伪水印引擎**：
  - 严格对齐官方 Canvas 阵列平铺排版（倾斜角度、字号与透明度）；
  - 自动基于 EXIF 方向标签进行图片旋转校正；
  - 动态质量自适应压缩算法，确保上传图片严格控制在 1MB 阈值内。
- **跨平台定时与防风控套件**：
  - Linux `run_cron.sh`：自动切换工作目录、匹配 Python 解释器，加入 10~180 秒随机延时；
  - Windows `run_windows.bat` & `run_silent.vbs`：支持任务计划程序后台无弹窗静默运行；
  - 节假日/调休暂停支持：检测到 `.pause` 标识文件自动跳过执行；
  - 统一日志输出至 `logs/cron.log`。
